extends ./layout/_base-layout.pug
block var
  include ./data/_data.pug
  -
    var ttl= global.siteName
    var title = 'user'
block content
  p Hello, #{USER}!!
  p
    | あなたは
    span.group
    | です。

  if USER != ''
    form(action='/logout' method='GET')
      input(type='submit' value='logout')
